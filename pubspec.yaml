name: anoopastro_light
description: A light kundali app with AnoopAstro branding.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  vedic_astrology: ^0.0.4
  intl: ^0.20.2
  google_fonts: ^6.3.1
  pdf: ^3.11.3
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
