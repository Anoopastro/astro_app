name: anoopastro_app
description: Limited astrology app like AstroSage
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  pdf: ^3.11.0
  printing: ^5.10.0
  flutter_svg: ^2.1.0   # <- downgraded
  cupertino_icons: ^1.0.5
